# Testing

Angular unit tests should live next to the source files they test once the Angular workspace is generated. End-to-end tests may live in the Angular workspace convention or in `tests/` if a separate test runner is adopted.

Until the workspace exists, `tests/` is reserved for repository-level, integration, or e2e test scaffolding.
